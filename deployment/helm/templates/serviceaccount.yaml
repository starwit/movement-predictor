apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "serviceAccountName" . }}
  labels:
    {{- include "labels" . | nindent 4 }}
imagePullSecrets:
{{- with .Values.extraImagePullSecrets }}
  {{- toYaml . | nindent 2 }}
{{- end }}
